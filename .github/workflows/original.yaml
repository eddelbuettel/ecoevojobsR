## see https://github.com/mikeblazanin/ecoevojobsR/blob/e7bdefdce7429648916f9221a4177ccaf3f6f3ce/.github/workflows/schedule-pull-and-run.yaml
name: original

on:
  push:
    branches: [main, master]
  #schedule:
  #  - cron: "0 * * * *"

jobs:
  generate-data:
    runs-on: ${{ matrix.config.os }}

    name: ${{ matrix.config.os }} (${{ matrix.config.r }})

    strategy:
      fail-fast: false
      matrix:
        config:
          - {os: ubuntu-latest,   r: 'release'}

    env:
      R_REMOTES_NO_ERRORS_FROM_WARNINGS: true
      RSPM: ${{ matrix.config.rspm }}
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@v3

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}
          http-user-agent: ${{ matrix.config.http-user-agent }}
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2

      - name: Run script
        run: |
          source("R/ecoevo_jobs_analysis.R")
        shell: Rscript {0}

      # - name: Commit files
      #   run: |
      #     git config --local user.email "actions@github.com"
      #     git config --local user.name "GitHub Actions"
      #     git add --all
      #     git commit -am "Ran ecoevo_jobs_analysis.R & commit all changes"
      #     git push

      # - name: Session info for debugging
      #   run: |
      #     options(width = 100)
      #     pkgs <- installed.packages()[, "Package"]
      #     sessioninfo::session_info(pkgs, include_base = TRUE)
      #   shell: Rscript {0}

